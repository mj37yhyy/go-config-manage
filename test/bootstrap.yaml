application:
  config:
    file: true
    remote:
      - enabled: true
        format: yaml
        provider: consul
        endpoint:
          - localhost:8500
        path:
          - go-config
          - a/b/c
        token: sssss
